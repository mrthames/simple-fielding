# macOS / Windows
.DS_Store
Thumbs.db

# Claude Code
.claude/

# Node
node_modules/

# Playwright
test-results/
playwright-report/

# Assembled Pages site (built in CI)
_site/

# Native builds, once the iOS and Android shells exist
build/
DerivedData/
xcuserdata/
android/.gradle/
android/app/build/
android/local.properties
*.jks
keystore.properties

# Credentials and server details go nowhere: keys, env files, the deploy settings, SSH config.
.env
.env.*
deploy.env
*.pem
*.key
*.ppk
*.p12
id_rsa*
id_ed25519*
id_ecdsa*
known_hosts
.ssh/
# Local settings files (Claude Code's, editors'), whatever they're called
*local.json
